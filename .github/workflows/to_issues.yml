on:
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: "ubuntu-latest"
    steps:
      - uses: "actions/checkout@master"
      - name: "to issues"
        uses: "alstr/todo-to-issue-action@v4"
        id: "todo"