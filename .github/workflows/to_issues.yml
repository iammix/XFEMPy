on:
  push:
    branches:
      - XFEMPY-001
jobs:
  build:
    runs-on: "ubuntu-latest"
    steps:
      - uses: "actions/checkout@master"
      - name: "to issues"
        uses: "alstr/todo-to-issue-action@v4"
        id: "todo"